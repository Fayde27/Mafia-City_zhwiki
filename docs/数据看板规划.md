# 數據看板 — 需求規劃與實施方案

> 建立：2026-09-11 ｜ 狀態：**規劃完成，尚未開工（代碼一行未動）**
>
> 開工前還有 2 個待拍板事項，見第七節。

---

## 一、需求來源（用戶原話整理）

**總體：**
1. 網址的訪問量
2. 不同模塊各自的點擊量（陣容搭配、玩法攻略、道具介紹…）
3. 文章的點擊量 —— ✅ 已做

**細部：**
- 陣容 → 不同流派的點擊量
- 道具 → 不同道具的點擊量

**其他：**
- 主頁搜索的訪問量 + 搜索記錄彙總

**呈現方式（用戶明確要求）：**
- 做成**獨立的數據看板頁面**，而不是把小數字散落在各模塊裡
- 除了總量，也要能**按日 / 週 / 月**切換查看

---

## 二、現狀盤點（已查證，新會話不必重查）

| 項目 | 現狀 | 相關檔案 |
|---|---|---|
| 文章瀏覽 | ✅ 已做。`Article.views`（對外展示，隨機 +1~5 注水）＋ `realViews`（真實數，每次 +1，僅後台） | `src/app/api/wiki/view/route.ts` |
| 瀏覽寫入方式 | RPC `increment_article_views` 原子遞增；函數不存在時自動退回「先 select 再 update」 | 同上 |
| 道具 / 活動 / 陣容 | ❌ 表中**無任何瀏覽欄位**，完全沒統計 | `prisma/schema.prisma` |
| 流派點擊 | ❌ 無 | — |
| 搜索 | ❌ 完全沒記錄。頂欄與首頁搜索框只是 `router.push('/wiki/search?q=…')` | `src/components/WikiHeader.tsx`、`src/app/page.tsx` |
| 整站 PV | ❌ 無自建統計 | — |
| 時間維度 | ❌ 現有機制**做不到**（見第三節） | — |

**環境前提：**
- Cloudflare Pages 免費版部署；Bot Fight Mode 已開、AI 爬蟲已 Block（見記憶 `mafiacity-cloudflare-security`）
- 搜索關鍵字清洗函數已存在：`src/lib/sanitize.ts` 的 `sanitizeSearch`
- ⚠️ `supabase-migrations/2026-09-article-real-views.sql` **是否已在 Supabase 執行，尚未確認**。未執行則 `realViews` 不會累計（但前台瀏覽數正常，有退路）

---

## 三、關鍵架構決策

### 現有「在表上加計數列」的做法做不了日/週/月
計數列只存一個數字，沒有時間資訊。要按日週月，**必須換成按天聚合表**。

### 地基：一張按天聚合表

```
DailyStat(date, metric, key, count)

2026-09-10 | page   | /wiki/lineups | 120
2026-09-10 | item   | item_abc      |  30
2026-09-10 | genre  | 飛車黨         |  45
2026-09-10 | search | (總次數)       | 203
```

- 一張表覆蓋**全部**維度，不必給每個模塊加列
- 日/週/月 = 「加總某段日期」，隨意切
- 行數可控：每天約數百行，一年數萬行
- 累計總數 = 全部相加；`Article.realViews` 可保留作快速讀取
- 寫入需 Postgres 函數做 upsert 原子遞增（參考已有的 `increment_article_views` 寫法）

### 搜索詞另開一張

```
SearchLog(date, keyword, count, hasResult)
```
`hasResult` 用來產出「**搜了但沒結果**」清單 —— 全看板最有價值的一塊，直接指出該補什麼內容。

### 不引入圖表庫
專案目前無圖表庫。`recharts` 之類會明顯增加打包體積（Cloudflare Pages 有體積考量）。
**條形圖用 CSS、折線用手寫 SVG**，零依賴。

---

## 四、看板頁面設計

**路徑：** `/admin/analytics`（後台首頁加入口）
**取數 API：** `/api/admin/analytics?range=7d`（從 DailyStat 聚合）

```
┌──────────────────────────────────────────────────────────────┐
│  數據看板            [今日] [近7天] [近30天] [全部] [自訂區間] │
├──────────────────────────────────────────────────────────────┤
│  總瀏覽 12,480    搜索 1,203    最熱模塊 陣容搭配    最熱內容 …│
├──────────────────────────────────────────────────────────────┤
│  模塊對比                                                     │
│    陣容搭配  ████████████████████  5,240                      │
│    玩法攻略  ████████████          3,110                      │
│    道具介紹  ███████               1,890                      │
│    活動介紹  ████                    980                      │
├──────────────────────────────────────────────────────────────┤
│  趨勢（按天折線）                                              │
├───────────────────────────┬──────────────────────────────────┤
│  內容排行 Top 10          │  搜索                             │
│  [文章][道具][活動][流派] │  熱搜詞 Top 20                    │
│                           │  ⚠️ 搜了但沒結果的詞（要補內容）  │
└───────────────────────────┴──────────────────────────────────┘
```

**四塊：** ①總覽卡片 ②模塊對比條形圖 ③按天趨勢 ④內容排行＋搜索

### 各模塊裡的小數據怎麼辦
**保留但精簡**（編輯時順手要看）：
- 文章列表「瀏覽」欄 → 保留（已做）
- 文章列表上方的概況條 → 可撤掉，改成「詳細數據見 數據看板 →」
- 道具 / 活動列表 → 不加數字，直接引導去看板

---

## 五、各需求的可行性與口徑

| 需求 | 難度 | 說明 |
|---|---|---|
| 整站 PV | 低 | DailyStat 的 `page` metric |
| 模塊點擊 | 低 | 各模塊是不同 URL：`/wiki/lineups`、`/wiki/guides`、`/wiki/items`、`/wiki/events` |
| 文章點擊 | ✅ 已做 | 接入 DailyStat 後可再獲得日/週/月趨勢 |
| 道具點擊 | 低 | 有獨立詳情頁 `/wiki/items/[slug]`，與文章同理 |
| 搜索量＋熱搜詞 | 中 | 需 SearchLog；注意空搜索/刷新去重、關鍵字長度截斷 |
| **流派點擊** | ⚠️ **口徑有坑** | 見下 |

### ⚠️ 流派點擊的口徑問題
流派是 `/wiki/lineups` **頁內的篩選按鈕，不換 URL**，因此只能統計「點篩選按鈕的次數」，這有兩個偏差：
- 預設是「全部流派」，使用者不點也能看到全部陣容 → **只統計到主動篩選的人**
- 反映的是「多少人想專門看這個流派」，**不等於流派受歡迎程度**

若要真實反映受歡迎程度，得改為統計每張陣容卡的曝光（可視區偵測），複雜度高一檔。

### 關於 Cloudflare Web Analytics
Cloudflare Pages 免費自帶整站 PV／各 URL 訪問量／來源／裝置，不用寫碼、bot 過濾更準。
**但它的數據在 Cloudflare 後台，無法方便拉進自建看板**（需 GraphQL API + Token）。
既然要「一個頁面看完」，**看板數字全部自建**，Cloudflare Analytics 留作交叉核對（判斷自建數字有沒有被 bot 灌水）。

---

## 六、分期建議

**一期（建議先做）**
1. `DailyStat` 表 + upsert 原子遞增函數
2. `SearchLog` 表
3. 前台埋點：模塊 PV、道具詳情 PV、搜索
4. `/admin/analytics` 看板頁：總覽卡片、模塊對比、按天趨勢、內容排行、熱搜詞

**二期**
- 流派篩選點擊
- 文章趨勢圖細化
- 「搜了沒結果」清單

**不做**（交給 Cloudflare，自建成本高且不準）
- 獨立訪客數 UV、來源渠道、裝置分布

---

## 七、⚠️ 開工前待拍板（2 項）

1. **流派點擊的口徑**
   - A：主動點篩選的次數（便宜，**建議**）
   - B：陣容曝光度（需可視區偵測，貴一檔）

2. **要不要做防刷**
   - 現在文章是刷新一次就 +1
   - 可加「同一瀏覽器 30 分鐘內同一對象只記一次」（sessionStorage/localStorage），數據會真實很多

（原本還有「Cloudflare 還是自建」「數據保留多久」兩項：前者已由「做成獨立看板」這個決定解決 → 全自建；後者建議按天聚合保留 1~2 年。）

---

## 八、實施時必須遵守（來自 CLAUDE.md）

1. 所有 route 檔頂部加 `export const runtime = 'edge'`
2. API 只用 `supabaseAdmin`，不在 runtime 用 Prisma Client
3. 新增欄位/表**必須手動在 Supabase SQL Editor 執行**，再同步 `prisma/schema.prisma`
4. JSON 欄位：DB 存字串，寫 `JSON.stringify()`、讀 `try/catch JSON.parse()`
5. 遷移 SQL 放 `supabase-migrations/`，命名如 `2026-09-analytics.sql`
